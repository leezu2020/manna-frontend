export const state = () => {}

export const mutations = {}

export const actions = {
    addPromise({commit}, promise){
        console.log('addPromise 시작', promise)
        // const res = await this.$axios.post('/api/promise', promise)
    }
}
